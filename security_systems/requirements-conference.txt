# Exact dependency versions from the Python 3.14.6 macOS ARM64 rehearsal.
# Constraints, not a cross-platform hash lock or a claim of testing every Python version.
cffi==2.1.1
cryptography==50.0.1
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pycparser==3.0
Pygments==2.21.0
pytest==9.1.1
PyYAML==6.0.3
ruff==0.16.8
