"""The kernel: independent mediators that no world, pack, or model can weaken.

Every module here is domain-neutral. A world supplies the cast and a pack supplies
the policy; the kernel supplies the checks. See ``docs/ARCHITECTURE.md``.
"""
