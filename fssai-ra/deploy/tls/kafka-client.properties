# Used by the broker health check and by operators running Kafka CLI tools.
security.protocol=SSL
ssl.truststore.type=PEM
ssl.truststore.location=/tls/ca.crt
# The broker requires a client certificate (mutual TLS).
ssl.keystore.type=PEM
ssl.keystore.location=/tls/kafka-client-admin.pem
