"""Repository maintenance scripts importable by tests and downstream tooling.

Keeping this directory as a package makes ``pytest`` and ``python -m pytest``
behave identically. The command-line entry points remain directly executable.
"""
